<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- merged into the consumer app; runtime grant still required on API 33+ -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <application>
    <activity
      android:name="com.margelo.nitro.obitrain.reactnativenotifications.NotificationOpenedActivity"
      android:exported="false"
      android:excludeFromRecents="true"
      android:noHistory="true"
      android:taskAffinity=""
      android:theme="@android:style/Theme.Translucent.NoTitleBar" />
    <service
      android:name="com.margelo.nitro.obitrain.reactnativenotifications.ObiMessagingService"
      android:exported="false">
      <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
      </intent-filter>
    </service>
  </application>
</manifest>
